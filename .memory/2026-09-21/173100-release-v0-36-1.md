# Release v0.36.1 — Share link RPC parse fix

- **Task:** Patch bump `0.36.0+91` → `0.36.1+92` (bugfix: `createShareToken`
  `rpc<Map>` → `rpc<List>` untuk RPC `RETURNS TABLE`), commit, tag `v0.36.1`
  (annotated, ikut konvensi `v0.36.0`), push main + tag. Isi rilis sejak
  `v0.36.0`: exact-edit fix parse `create_bill_share_token`, 3 regression
  test datasource via fake manual (`FakeSupabaseClient`), docs plan/memory
  yang disinkronkan hasil review.
- **Key files:** `pubspec.yaml` (version),
  `lib/data/datasources/bill_remote_datasource.dart` (fix),
  `test/data/datasources/bill_remote_datasource_share_token_test.dart`
  (regression), `plans/2026-09-21-fix-share-link-rpc-parse-plan.md`,
  `.memory/README.md` (index).
- **Submodule:** pointer tidak berubah (tetap `62a6bf9`);
  `? supabase` di status parent hanya penanda untracked pre-existing, bukan
  perubahan pointer.
- **Verification:** `flutter analyze --no-fatal-infos` 0 error/0 warning
  (163 info pre-existing), `dart run build_runner build
  --delete-conflicting-outputs` fresh (75 outputs, tanpa diff generated),
  test baru 3/3, terkait 22/22 (notifier lama tidak diubah),
  `flutter test` penuh 673/673 hijau. Smoke manual emulator tetap pending
  operator. `.env.local` tidak tersentuh (gitignored ✓), tidak ada secret
  di commit mana pun.
- **Decisions:** tag annotated `v0.36.1` memicu workflow Release Android
  Artifacts (build bertanda upload-key + upload AAB ke Play internal
  testing) — sesuai desain repo.
