# Release v0.35.0 — Share-Link Web Publik

- **Task:** Minor bump `0.34.0+89` → `0.35.0+90` (fitur), commit, tag `v0.35.0` (annotated,
  ikut konvensi `v0.29.0`), push main + tag. Isi rilis sejak `v0.34.0`: share-link https
  `/s/`, kuota global Free 1 / Plus 5 FIFO + guard downgrade, peringatan in-app, halaman
  landing `/s/:token`, migration `20260917000000` (sudah TER-APPLY produksi + koreksi
  resolve terverifikasi).
- **Key files:** `pubspec.yaml` (version saja).
- **Verification:** `flutter pub get`, `flutter analyze` 0e/0w, `flutter test` 670 passed,
  `flutter build apk --split-per-abi` 3 APK release lokal (armeabi-v7a/arm64-v8a/x86_64,
  debug-signed; rilis resmi + upload Play internal-testing dikerjakan CI Release workflow
  saat tag di-push).
- **Decisions:** tag annotated `v0.35.0` memicu workflow Release Android Artifacts
  (build bertanda upload-key + upload AAB ke Play internal testing) — sesuai desain repo.
- **Commit:** `chore: bump version to 0.35.0+90` + tag `v0.35.0`, keduanya ter-push.
